import React from 'react';
//import ReactDOM from 'react-dom';

import Input from './Input';

export default class Footer extends React.Component{
  render() {
    return (
      <div>
        <Input value='Footer'/>
      </div>
    );
  }
}
//ReactDOM.render(<Footer/>, document.getElementById('text3'));
