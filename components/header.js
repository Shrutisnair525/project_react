import React from 'react';
//import ReactDOM from 'react-dom';

import Input from './Input';

export default class Header extends React.Component{
  render() {
    return (
      <Input value='Header'/>
    );
  }
}
//ReactDOM.render(<Header/>, document.getElementById('text1'));
